# Supabase yapilandirmasi
# Bulut projesine baglamak icin:  npx supabase link --project-ref <ref>
# Migration'lari gondermek icin:  npx supabase db push

project_id = "adisyon"

[api]
enabled = true
port = 54321
schemas = ["public", "storage"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10

# Tek kullanicili sistem: kayit ekrani yok, hesabi biz olusturuyoruz.
enable_signup = false

[auth.email]
enable_signup = false
enable_confirmations = false

[storage]
enabled = true
file_size_limit = "5MiB"
